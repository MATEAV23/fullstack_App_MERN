import type { Task } from "@/types/index"

type TaskCardProps = {
    task: Task
}

const TaskCard = ({task}: TaskCardProps) => {
  return (
    <div>
      pepe
    </div>
  )
}

export default TaskCard
